name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npx ng build --base-href=/brain-cache/

      - name: Configure git
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"

      - name: Deploy to gh-pages
        run: npx angular-cli-ghpages --dir=dist/brain-cache/browser --repo=https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/Studying-xXx-GloriousPhoenix-xXx/brain-cache.git